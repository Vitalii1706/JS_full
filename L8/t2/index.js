'use strict';

const sortDesc = (arr) => arr.slice().sort((a, b) => a - b);
const arr = [1, 3, 5, 8, 4, 2, 6, 9, 7, 2, 6, 9];
console.log(sortDesc(arr));
console.log(arr);

function run() {
  var a = 0,
    b = 1;

  if (a == b) {
    return c;
  }
}

function run() {
  var a = 0,
    b = 1;

  if (a == b) {
    return c;
  }
}
